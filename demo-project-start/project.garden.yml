kind: Project
name: demo-project-start
environments:
  - name: preview
    defaultNamespace: '${local.username}-$GITHUB_HEAD_REF-preview'
    variables:
      hostname: '${var.previewHostname}'
providers:
  - name: kubernetes
    context: gke_starry-minutia-376412_europe-north1-a_demo-preview-environments
    environments:
      - preview
    namespace: '${environment.namespace}'
    defaultHostname: '${var.hostname}'
    deploymentRegistry:
      hostname: eu.gcr.io
      namespace: starry-minutia-376412
    imagePullSecrets:
      - name: gcr-config
        namespace: default
variables:
  previewHostname: $GITHUB_HEAD_REF.${{ vars.INGRESS_IP }}
