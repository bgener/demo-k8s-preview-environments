kind: Project
name: demo-project-start
environments:
  - name: preview
    defaultNamespace: '${local.username}-$PR_NUMBER-preview'
    variables:
      hostname: '${var.previewHostname}'
providers:
  - name: kubernetes
    context: gke_starry-minutia-376412_europe-north1-a_demo-preview-environments
    environments:
      - preview
    namespace: '${environment.namespace}'
    defaultHostname: '${var.hostname}'
    deploymentRegistry:
      hostname: eu.gcr.io
      namespace: starry-minutia-376412
    imagePullSecrets:
      - name: gcr-config
        namespace: default
variables:
  previewHostname: '$PR_NUMBER.$INGRESS_IP'
